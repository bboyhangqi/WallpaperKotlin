<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 线的宽度，颜色灰色 -->
    <stroke android:width="1dp" android:color="@color/dialog_presentation_bg"></stroke>

    <!-- 矩形的圆角半径 -->
    <corners android:radius="8dp" />

    <gradient
        android:endColor="@color/dialog_presentation_bg"
        android:startColor="@color/dialog_presentation_bg" /><!-- 矩形的内部颜色，这里设置成白色，即空心 -->

</shape>